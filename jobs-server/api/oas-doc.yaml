openapi: 3.0.3
info:
  version: 1.0.0
  title: Autogenerated Api
  description: Generated with OAS TOOLS CLI 

paths:
  /api/v1/jobss:
    x-router-controller: jobsController
    get:
      description: Returns all jobss
      operationId: getJobss
      responses:
        200:
          description: Jobs list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/jobs'
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/error'
  /api/v1/jobss/{province}:
    x-router-controller: jobsController
    get:
      description: Returns a single jobs by its province
      operationId: findByprovince
      parameters:
        - required: true
          name: province
          description: province of the jobs to fetch
          in: path
          schema:
            type: string
      responses:
        200:
          description: Jobs found by province
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/jobs'
        default:
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/error'

components:
  schemas:
    jobs:
        type: object
        required:
          - province
        properties:
          province:
            type: string
          year:
            type: number
          gender:
            type: string
          low_due_to_placement:
            type: number
          no_renovation:
            type: number
          other_reason:
            type: number
    error:
      type: object
      required:
        - message
      properties:
        message:
          type: string
      additionalProperties: true